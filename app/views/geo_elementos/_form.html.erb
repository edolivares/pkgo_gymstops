<%= form_for(geo_elemento) do |f| %>
  <% if geo_elemento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(geo_elemento.errors.count, "error") %> prohibited this geo_elemento from being saved:</h2>

      <ul>
      <% geo_elemento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :latitude %>
    <%= f.text_field :latitude %>
  </div>

  <div class="field">
    <%= f.label :longitude %>
    <%= f.text_field :longitude %>
  </div>

  <div class="field">
    <%= f.label :tipo %>
    <%= f.number_field :tipo %>
  </div>

  <div class="field">
    <%= f.label :votes %>
    <%= f.number_field :votes %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
